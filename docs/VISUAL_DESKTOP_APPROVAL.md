# Signierte visuelle Desktop-Abnahme

Die manuelle Abnahme wird erst nach realer Desktopprüfung erzeugt. Sie enthält Build-ID, Prüfer, Datum, Hash des Prüfmanifests, Hash aller Referenzbilder und Hash des visuellen Prüfberichts. Jede relevante Änderung lässt den Nachweis ablaufen.
